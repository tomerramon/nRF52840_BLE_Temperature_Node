# nRF52840_BLE_Temperature_Node
BLE peripheral firmware for nRF52840 built with nRF Connect SDK (Zephyr). Reads the internal temperature sensor and exposes it via a custom GATT service with temperature notifications and configurable sampling interval. Features moving average filter, min/max tracking, dynamic device naming, and watchdog support.
